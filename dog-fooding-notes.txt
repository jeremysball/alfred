- status line breaking into two lines 
- remove rich formatting and instead use 
  the ansi constants for tool call title 
  formatting 
- tool calls should show the beginning
  x characters not the end x characters
- default context size should be 128k. 
  is that an env var or in config?:
- completion menu does not disappear when 
  clearing input field
